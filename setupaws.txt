# Start with 

export KOPS_CLUSTER_NAME=vstest.k8s3
export KOPS_STATE_STORE=s3://cngi-kops-state

kops create cluster --name=${KOPS_CLUSTER_NAME} --cloud=aws --node-count=0 --control-plane-size=t3.small --node-size=m5dn.xlarge --zones=us-east-1a --cloud-labels "budget=cngi"

# You get the suggestions

Suggestions:
 * list clusters with: kops get cluster
 * edit this cluster with: kops edit cluster vstest.k8s3
 * edit your node instance group: kops edit ig --name=vstest.k8s3 nodes-us-east-1a
 * edit your control-plane instance group: kops edit ig --name=vstest.k8s3 control-plane-us-east-1a

# Skip this for now
# Finally configure your cluster with: kops update cluster --name vstest.k8s3 --yes --admin

# Add workers instancegroup

# kops create instancegroup workers

# Update the instance group configuration to have as many nodes as you would like (for example 1 node, 2 workers)

# Load the configuration from the .cfg files

kops replace -f kops_master.cfg
kops create -f kops_workers.cfg
kops replace -f kops_nodes.cfg

# Update cluster

kops update cluster --name vstest.k8s3 --yes --admin
kops validate cluster --wait 10m
kops rolling-update cluster

# Deploy the Dask helm chart

helm repo add dask https://helm.dask.org/
helm install dask dask/dask --version 2024.1.1 -f config.yaml
# Install from a local clone
# helm install dask helm-chart/dask  -f helm-chart/dask/values.yaml

# Run the following to get the Helm instance information

 helm status dask
helm get all dask

# Run the exports provided by “helm status dask”

# Get the pods

kubectl get pods

# Login to the Jupyter pod

kubectl exec --stdin --tty dask-jupyter-56b866f9f-bc8cv  -- /bin/bash

# In the container do 

jupyter server list

# Get the jupyter server token in a single line

kubectl exec --stdin --tty `kubectl get pods | grep jupyter | awk '{print $1}'`  -- /opt/conda/bin/jupyter server list

# Get the token and use it to
# Log in to http://127.0.0.1:8082


# Forward the scheduler port
export DASK_SCHEDULER_UI_IP="127.0.0.1"
kubectl port-forward --namespace default svc/dask-scheduler  8787:80 &

# Browse the scheduler at localhost:8787



# Get all the service names

kubectl get deployment --all-namespaces

# Get the external LoadBalancer IP for the dashboard and jupyter
 
kubectl get svc --namespace default dask-scheduler
kubectl get svc --namespace default dask-jupyter
